# Expectativa: Errores sintácticos sucesivos en función recuperados limpiamente #
# Prueba: Errores en cascada a lo largo de una función de cálculo completa (Consigna 14) #

int fcalc(int n, int base)
{
    int res, i;
    res = 0;
    i = 0;

    while(i < n
    {
        res = res + * 2;
        i = i + 1;
    }

    return res
}

void main()
{
    int val;
    val = fcalc(5, 10);
    cout << val;
}
